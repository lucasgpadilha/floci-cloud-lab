"""Ops workbench contracts for Floci Studio.

This package keeps product-level debugger contracts separate from the HTTP
adapter so future adapters can reuse traces, reports, and session metadata.
"""
